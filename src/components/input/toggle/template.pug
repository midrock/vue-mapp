.vm-toggle(
    :disabled="status === 'disabled'"
    :class="{
        'is--checked': emitValue,
        'is--right': iconRight    
    }"
    @click="toggle"
)
    .vm-toggle__thumb
    .vm-toggle__label(
        v-if="$slots.default || label"
    ) {{ label }}
        slot
