language: node_js
node_js:
  - "8"

script:
  - yarn run build

cache:
  yarn: true

deploy:
  - provider: npm
    email: "iteam4u@ya.ru"
    api_token: "$NPM_KEY"
    skip_cleanup: true
    tag: next
    edge: true
    on:
      branch: dev
      repo: midrock/vue-mapp